<style>
  .hero {
    min-height: 100vh;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 15px;
    padding: 0;
  }

  p {
    max-width: 500px;
    line-height: 1.3;
    color: var(--fc-paragraph);
  }

  h1 {
    font-size: clamp(1.125rem, 5vw, 1.25rem);
    color: var(--secondary-color);
  }

  .my-name {
    font-size: clamp(2.5rem, 8vw, 5rem);
  }

  h3 {
    font-size: clamp(2.5rem, 8vw, 4.5rem);
    color: var(--fc-paragraph);
  }
</style>

<section class="hero">
  <h1 class="hero-field hidden" style="--order: 5;">Hi, my name is</h1>
  <h2 class="hero-field my-name hidden" style="--order: 6;">Brayan Sanchez.</h2>
  <h3 class="hero-field hidden" style="--order: 7;">Always learning to be better.</h3>
  <p class="hero-field hidden" style="--order: 8;">
    I'm a <strong>junior software developer</strong> focused on continuous learning to build better
    and more efficient web applications. I'm currently improving my skills with
    the <strong>PERN</strong> stack and other technologies to become a full-stack developer.
  </p>
</section>
